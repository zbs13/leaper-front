export const initialState = {
    
};
  
  /**
   * action = {type: String, payload: any}
   */
  export const reducer = (state, action) => {
    switch (action.type) {
        default:
            return state;
    }
  };
  